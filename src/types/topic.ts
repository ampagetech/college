export interface Topic {
  id: string;
  name: string; // Ensure this exists
  topic: string;
  topic_long: string;
  content: string;
  activity: string;
  // Add any other properties that your topics have
}